version: "2"
linters:
  enable:
    - bodyclose
    - err113
    - gocritic
    - gocyclo
    - gosec
    - misspell
    - noctx
    - nolintlint
    - revive
    - sloglint
  settings:
    staticcheck:
      checks:
        - all # Enables all Staticcheck checks by default
formatters:
  enable:
    - gofmt
